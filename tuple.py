# tuple is same as a list
# It is defined as parentheses () where items are separated by commas.
# Tuple is faster as compare to list.
# If you are putting only one value then it is not count as tuple

t = (10, 30, 40, 50)
print(t, type(t))
m = min(t)
print(m)
max = max(t)
print(max)